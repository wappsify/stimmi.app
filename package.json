{
  "name": "stimmi-monorepo",
  "private": true,
  "scripts": {
    "pre-commit": "bun --filter '*' pre-commit",
    "dev": "bun --filter '*' dev",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "9.1.7"
  },
  "peerDependencies": {
    "typescript": "5.7.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
